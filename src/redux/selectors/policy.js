//create Paragraphs

export const first = policy => `
The company name is ${policy.companyName}.`;

export const second = policy => `
There are  ${policy.numberOfEmployees} employees.
`;
export const third = policy => `
The budget is ${policy.budget}.
`;
